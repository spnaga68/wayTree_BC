# MongoDB Configuration
# Replace with your actual MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/waytree?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production

# Server Configuration
PORT=3000
NODE_ENV=development
APP_MODE=development
DEBUG_MODE=false

# API URLs
API_BASE_URL=http://localhost:3000
FRONTEND_URL=http://localhost:8080

# CORS Origins (comma-separated)
CORS_ORIGIN=http://localhost:3000,http://localhost:8080,http://localhost:5000

# OTP Configuration
OTP_EXPIRY_MINUTES=10
OTP_CODE_LENGTH=6

# SMTP Configuration (for production email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_NAME=GoalNet
SMTP_FROM_EMAIL=

# Groq API Key (for AI features)
GROQ_API_KEY=
